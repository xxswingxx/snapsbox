.large-3.medium-3.small-12.columns.template-wrap.left
  = link_to image_tag(button.screenshot.thumb.url), button_path(button.id)  
  %p.thumb-info
    = link_to button.title, button_path(button.id)
    by
    %span.notes= link_to button.user.name